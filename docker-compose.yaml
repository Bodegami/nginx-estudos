version: '3'

services:

    web:
      image: nginx
      container_name: nginx
      hostname: nginx
      volumes:
        - "./html:/var/www/html/"
        - "./servers:/etc/nginx/conf.d/servers"
      ports:
        - "80:80"
        - "8080:8080"
        - "8888:8888"